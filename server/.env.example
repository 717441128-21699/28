# ========================================
# 车易拍后端数据库配置
# ========================================
# MySQL 配置（生产/推荐）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=cheyipai123
DB_NAME=cheyipai

# 服务配置
PORT=3000
JWT_SECRET=cheyipai-jwt-secret-key-2024

# 如果本地没有 MySQL，可设置为 true 使用 SQLite 兼容模式（仅用于临时开发）
USE_SQLITE_FALLBACK=false
